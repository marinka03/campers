export const selectorCatalogItems = (state) => state.catalog.items;
export const selectorAllItems = (state) => state.catalog.allItems;
export const selectorFavoriteItems = (state) => state.catalog.favorites;
